{
  "name": "realm-of-idlers",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "ready": "vp fmt && vp lint && vp run test -r && vp run build -r",
    "dev": "vp run @realm-of-idlers/game#dev",
    "prepare": "vp config"
  },
  "devDependencies": {
    "@google/genai": "catalog:",
    "@types/mime": "catalog:",
    "mime": "catalog:",
    "sharp": "catalog:",
    "simplex-noise": "catalog:",
    "tsx": "catalog:",
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@10.33.0"
}
